# Core dependencies for Axiom Python bindings
# Note: pyo3, tokio, serde, etc. are Rust crates, not Python packages
# They are handled by the Rust build system (maturin)

# Development dependencies
pytest>=7.0
pytest-asyncio>=0.21
black>=23.0
isort>=5.12
mypy>=1.0
ruff>=0.1

# Optional dependencies for demos
jupyter>=1.0
matplotlib>=3.5
numpy>=1.21
pandas>=1.3
requests>=2.28
